```
ErettSeGit - Informatics 'Érettségi' Downloader

usage: erettsegit.py [-h] [--interactive] YEAR MONTH LEVEL

positional arguments:
  YEAR               year
  MONTH              month
  LEVEL              level

optional arguments:
  -h, --help         show this help message and exit
  --interactive, -i  defaults to non-interactive
```
